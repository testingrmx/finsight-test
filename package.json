{
  "name": "finsight",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run srv\" \"npm run cli\"",
    "srv": "cd server && npm run dev",
    "cli": "cd client && npm run dev",
    "setup": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": { "concurrently": "^8.2.2" }
}
